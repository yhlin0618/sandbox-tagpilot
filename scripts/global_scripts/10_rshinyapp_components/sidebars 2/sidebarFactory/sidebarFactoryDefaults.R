#' Default Values for Sidebar Factory Component
#'
#' This file provides default values for the sidebar factory component.
#' These defaults ensure that all UI outputs have appropriate values even when
#' data is unavailable or invalid, implementing the UI-Server Pairing Rule.
#'
#' @return Named list of default values
#' @export
sidebarFactoryDefaults <- function() {
  list(
    # No specific defaults needed for the factory itself
    # Each specific sidebar has its own defaults file
  )
}