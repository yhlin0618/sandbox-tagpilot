#' Default Values for Micro Transactions Component
#'
#' This file provides standard default values for the micro transactions component.
#' These defaults ensure that all UI outputs have appropriate values even when
#' data is unavailable or invalid, implementing the UI-Server Pairing Rule.
#'
#' @return Named list of output IDs and their default values
#' @export
microTransactionsDefaults <- function() {
  list(
    # Currently no default outputs defined
    # Add defaults here as needed when UI outputs are added
  )
}